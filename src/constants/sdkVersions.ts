/** Current published / documented server SDK versions (keep in sync with package manifests). */
export const SDK_VERSIONS = {
  typescript: '0.1.8',
  nestjs: '0.1.0',
  // Legacy SDK versions remain recorded here for a future relaunch.
  // java: '0.1.8',
  // go: 'v0.1.8',
  // dotnet: '0.1.8',
} as const;
